# Maintainers

| GitHub | Declared interest |
|---|---|
| astrogilda | Maintains `aee-conformance` (the reference verifier and conformance suite for the adversarial-execution-evidence predicate) and its reference implementation, which appear as an issuer in this registry. Per `GOVERNANCE.md`, crosswalks for `aee-conformance` are reviewed by a maintainer other than this one -- currently unassigned; a second maintainer is needed before any such crosswalk can merge. |

Adding a maintainer requires the process in `GOVERNANCE.md` ("Committer access") and a disclosed-interest row in this table before that person reviews anything.
